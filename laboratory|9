<!DOCTYPE html>
<html>
  <body>
    <script>
  
      let ul = document.createElement("ul");
      document.body.append(ul);
      let cookies = document.cookie.split(";");
      for (let i = 0; i < cookies.length; i++) {
        let li = document.createElement("li");
        li.textContent = cookies[i];
        ul.append(li);
      }
    </script>
  </body>
</html>
